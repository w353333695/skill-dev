name: batchDeleteInstance
module: cmdb_instance
method: DELETE
path: /object/{objectId}/instance/_batch
gateway_path: ''
service: logic.cmdb.service
side_effect: delete
path_source: backend_contract
path_confidence: high
tags:
- CMDB
- 实例
- 删除
- 批量
summary: 批量删除实例
description: '按 objectId 与分号分隔的 instanceIds 批量删除实例（硬删除，不可逆），

  返回删除失败的实例列表（deleteFailedInstances，FailInstance[]）。

  ⚠️ 来源说明：前端 openapi 未录制此接口，卡片由后端契约（contracts.yaml:60587）

  直接生成，path_source=backend_contract / path_confidence=high。

  ⚠️ instanceIds 传参位置（query 还是 body）契约未明确（request.fields 无 in 标注），

  按 DELETE + 分号字符串惯例推测为 query（?instanceIds=id1;id2），首次真调需确认。

  '
request:
  required:
  - objectId
  - instanceIds
  properties:
    objectId:
      type: object_id
      desc: 模型对象ID（path 占位符 {objectId}）
    instanceIds:
      type: string
      desc: 实例ID，多个用分号隔开，e.g. 5ab9f10a6ef88;5abb0dca9971d（推测以 query 传递）
outputs:
  deleteFailedInstances:
    jsonpath: $.data.deleteFailedInstances
    desc: 删除失败的实例列表（FailInstance[]）
requires:
- objectId 须为已存在的模型对象ID
- instanceIds 中每个 id 须为该 objectId 模型下已存在的实例
- 硬删除不可逆，无回滚接口（rollback 留空）
rollback: null
examples:
- objectId: HOST
  instanceIds: 5ab9f10a6ef88;5abb0dca9971d
confidence:
  request: high
  outputs: high
  module: high
  tags: high
  summary: high
  requires: medium
  rollback: low
endpoint:
  contract_ref: logic.cmdb.service|DELETE|/object/{objectId}/instance/_batch
  mode: easyops_internal
source: {}
registered_at: '2026-07-24T10:51:14.675818'
