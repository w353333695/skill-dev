name: setFormMainVersion
module: form
method: POST
path: /api/flowable_service/v1/form/{formId}/version/{versionId}
service: logic.flowable_service
side_effect: update
path_source: manual
path_confidence: high
tags:
- 表单
- 设主
- 主版本
summary: 设表单主版本(内网直连,无setMain后缀)
description: 设表单主版本。POST /v1/form/{formId}/version/{versionId}（⚠️直接POST到版本URL,不带/setMain后缀;内网8134直连可达）。⚠️注意:registry/form/setMainVersion卡片的path(/setMain后缀)是网关路由,内网404,本卡片才是内网正确路由。2026-07-28实测。详见 form_development §4 配套接口。
request:
  required:
  - formId
  - versionId
  properties:
    formId:
      type: string
      desc: 路径
    versionId:
      type: string
      desc: 要设主的版本ID
outputs:
  detail:
    jsonpath: $.data
    desc: 设主结果
rollback: null
examples: []
endpoint:
  contract_ref: logic.flowable_service|POST|/api/flowable_service/v1/form/{formId}/version/{versionId}
  mode: easyops_internal
