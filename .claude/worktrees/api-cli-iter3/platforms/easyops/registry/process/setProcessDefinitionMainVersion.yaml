name: setProcessDefinitionMainVersion
module: process
method: PUT
path: /api/flowable_service/v1/definition/{definitionId}/version/{versionId}
service: logic.flowable_service
side_effect: update
path_source: manual
path_confidence: high
tags:
- 流程
- 设主
- 主版本
summary: 设流程主版本(触发部署,内网直连)
description: 设流程主版本。PUT /v1/definition/{definitionId}/version/{versionId}（⚠️是/v1/definition/非/v2/，内网8134直连可达,设主触发flowable部署,超时设120s）。设主后版本isMain=True+生成deploymentId/flowableDefinitionId。⚠️定义列表层list_process_definition的isMain/deploymentId有延迟不同步(以版本层为准)。2026-07-28实测。详见 process_development §1规则2 + §8.2。
request:
  required:
  - definitionId
  - versionId
  properties:
    definitionId:
      type: string
      desc: 路径
    versionId:
      type: string
      desc: 要设主的版本ID
outputs:
  detail:
    jsonpath: $.data
    desc: 设主结果
rollback: null
examples: []
endpoint:
  contract_ref: logic.flowable_service|PUT|/api/flowable_service/v1/definition/{definitionId}/version/{versionId}
  mode: easyops_internal
