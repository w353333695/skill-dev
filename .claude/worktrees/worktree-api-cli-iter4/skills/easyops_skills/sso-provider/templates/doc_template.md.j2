# {{ provider_name }} SSO 对接说明

## 1. 概述

本文档描述 EasyOps 平台与 **{{ sso_name }}** 的 SSO 单点登录对接方案。

- **协议类型**：{{ protocol_type }}
- **Provider 标识**：`{{ provider_name }}`
- **对接方式**：通过 EasyOps SSO Adapter 插件机制实现

### 认证流程概览

```
用户 → EasyOps → SSO Adapter → {{ sso_name }}
         │                        │
         │   1. 发起登录请求       │
         │ ──────────────────────→│
         │                        │
         │   2. 用户在 SSO 认证    │
         │                        │
         │   3. 携带凭证回调       │
         │ ←──────────────────────│
         │                        │
         │   4. 换取用户信息       │
         │ ──────────────────────→│
         │                        │
         │   5. 登录成功           │
         │ ←──────────────────────│
```

## 2. 认证流程详解

### 2.1 登录流程

{% if protocol_type == 'OAuth2' or protocol_type == 'OIDC' %}
1. 用户访问 EasyOps，触发 SSO 登录
2. `pre_signin()` 构造 OAuth2 授权 URL，将用户重定向到 {{ sso_name }}
3. 用户在 {{ sso_name }} 完成认证后，携带 `code` 回调到 EasyOps
4. `signin()` 使用 `code` 向 {{ sso_name }} 的 token 端点换取 `access_token`
5. `user_info()` 使用 `access_token` 获取用户信息，提取唯一标识
{% elif protocol_type == 'CAS' %}
1. 用户访问 EasyOps，触发 SSO 登录
2. `pre_signin()` 构造 CAS 登录 URL，将用户重定向到 {{ sso_name }}
3. 用户在 {{ sso_name }} 完成认证后，携带 `ticket` 回调到 EasyOps
4. `signin()` 提取 `ticket`
5. `user_info()` 使用 `ticket` 向 {{ sso_name }} 验证并获取用户信息
{% else %}
1. 用户访问 EasyOps，触发 SSO 登录
2. `pre_signin()` 构造认证请求，将用户重定向到 {{ sso_name }}
3. 用户在 {{ sso_name }} 完成认证后，携带认证凭证回调到 EasyOps
4. `signin()` 解析并验证认证凭证
5. `user_info()` 根据凭证获取用户信息
{% endif %}

### 2.2 登出流程

1. 用户在 EasyOps 发起注销
2. `sign_out()` 返回 {{ sso_name }} 的注销 URL
3. 框架将用户重定向到 {{ sso_name }} 完成全局注销

## 3. 配置说明

配置文件路径：`settings/setting_custom.py`

| 配置项 | 类型 | 必填 | 说明 |
|--------|------|------|------|
{% for item in config_docs %}
| `{{ item.key }}` | {{ item.type }} | {{ item.required }} | {{ item.description }} |
{% endfor %}

### 配置示例

```python
PLUGIN_CONFIG = {
    "default": "{{ provider_name }}",
    "{{ provider_name }}": {
        {% for item in config_docs %}
        "{{ item.key }}": {{ item.example_value }},  # {{ item.description }}
        {% endfor %}
    },
}
```

## 4. 部署步骤

### 4.1 前置条件

- EasyOps SSO Adapter 已部署并正常运行
- 已从 {{ sso_name }} 管理端获取必要的认证信息（client_id、client_secret 等）
- 已在 {{ sso_name }} 配置回调地址

### 4.2 部署操作

```bash
# 1. 进入 sso_adapter 安装目录
cd /path/to/sso_adapter

# 2. 备份当前配置
cp settings/setting_custom.py settings/setting_custom.py.bak

# 3. 解压 provider 包（解压后的文件会放到正确的目录结构下）
unzip {{ provider_name }}_sso.zip

# 4. 编辑配置文件，填入实际的认证信息
vi settings/setting_custom.py

# 5. 重启 SSO Adapter 服务
cd /usr/local/easyops/sso_adapter && easyops init && easyops restart
```

### 4.3 验证

```bash
# 检查服务是否正常启动
curl http://localhost:8211/api/v1/pre-signin/{{ provider_name }}
```

预期返回包含 `redirectURL` 的 JSON 响应。

## 5. 接口映射

| SSO Adapter 接口 | Provider 方法 | 说明 |
|-------------------|---------------|------|
| `GET /api/v1/pre-signin/{{ provider_name }}` | `pre_signin()` | 获取登录跳转 URL |
| `GET /api/v1/signin/{{ provider_name }}` | `signin()` + `user_info()` | 完成登录并获取用户信息 |
| `POST /api/v1/sign-out/{{ provider_name }}` | `sign_out()` | 用户注销 |
| `POST /api/v1/sso_adapter/auth-post-deal/{{ provider_name }}` | `authorize_post_deal()` | 授权后处理 |
| `POST /api/v1/global/sign-out` | `sso_global_sign_out()` | 全局登出通知 |
| `POST /api/v1/custom_auth/password` | `custom_auth()` | 自定义密码校验 |

## 6. 注意事项

{% if notes %}
{% for note in notes %}
- {{ note }}
{% endfor %}
{% else %}
- 部署前请确保 `setting_custom.py` 中的配置项已填写实际值
- 建议先在测试环境验证对接成功后再部署到生产环境
- 如遇问题，检查 `log/sso_adapter.err` 日志文件
{% endif %}

## 7. 文件清单

```
{{ provider_name }}_sso.zip
├── settings/
│   └── setting_custom.py          # 配置文件
└── handlers/
    └── providers/
        └── {{ provider_name }}/
            ├── __init__.py         # 模块初始化
            └── {{ provider_name }}.py  # Provider 实现
```
