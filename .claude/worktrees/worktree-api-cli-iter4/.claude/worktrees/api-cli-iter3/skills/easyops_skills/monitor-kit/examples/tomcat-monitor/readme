- 采集对象为 TOMCAT@ONEMODEL 模型实例
- 通过 Tomcat Manager HTTP 接口采集运行状态
- 需要在 Tomcat 中配置 Manager 应用并设置访问用户

## 前置条件

1. Tomcat 需启用 Manager 应用
2. 配置 tomcat-users.xml 添加管理用户：
   ```xml
   <user username="monitor" password="password" roles="manager-status"/>
   ```
3. 如有访问限制，需在 manager/META-INF/context.xml 中放开IP限制

## 采集指标

- 服务状态 (status)
- JVM内存：空闲/已用/最大/使用率
- 线程池：最大/当前/繁忙/使用率
- 请求统计：请求数/错误数/最大处理时间

## 采集任务配置

监控对象建议通过条件动态筛选，如：is_monit = true
