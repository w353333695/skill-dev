# hub 平台商店实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `platforms/easyops/hub/` 从无序堆放目录改造成平台商店——开发前查 INDEX 查重、成品交付自动回流、lint 兜底索引一致性。

**Architecture:** 方案 B（集中式 reference）：新建 `references/hub.md` 集中定义机制（目录规范/INDEX schema/查重/回流），SKILL.md + orchestration.md + onboarding.md 挂最小指针；`lint-platforms.py` 加第 11 段 hub 校验；存量 48 文件清理 + 全量登记 INDEX.yaml。零新运行时代码——查重靠 LLM 读 INDEX 语义匹配，一致性靠 lint。

**Tech Stack:** Python 3（系统 python3 即可，依赖 PyYAML——lint 现有依赖，无新增）、YAML、bash/git。

**Spec:** `skills/api-orchestrator/docs/2026-09-07-hub-store-design.md`（含 2026-09-07 版本管理修正：history/based_on）

## Global Constraints

- 中文写文档/注释（AGENTS.md §1）。
- skill 本体零系统耦合：`references/hub.md` 不得出现 easyops 特有内容（具体类目语义只活在 easyops 的 INDEX.yaml 头注释里）。
- 所有 git commit 尾行加 `Co-Authored-By: Claude <noreply@anthropic.com>`。
- lint 新校验的退出码语义不变：0=无 ERR（可有 WARN），1=有 ERR。
- hub 校验只在 `hub/` 目录存在时跑；不存在时跳过且不报错（hub 是可选资产）。
- 编辑 `platforms/easyops/hub/` 下文件前如遇只读权限，先 `chmod -R u+w`，改完不锁回（hub 是高频回流域，与 onboarding 的锁回纪律不同；此差异在 hub.md 中声明）。
- 测试命令一律从仓库根 `/workspace` 用绝对/明确相对路径跑（AGENTS.md 记忆：go test 等单命令绝对路径；python 同理）。
- 本计划不动 `platforms/easyops/hub/` 之外任何 platforms 资产内容（README.md 例外：资料地图加一行）。

## 文件结构总览

| 文件 | 动作 | 职责 |
|---|---|---|
| `skills/api-orchestrator/references/hub.md` | 新建 | hub 机制唯一定义点：结构/INDEX schema/查重/回流（通用零耦合） |
| `skills/api-orchestrator/scripts/lint-platforms.py` | 修改 | 加第 11 段 hub 校验（8 条规则） |
| `skills/api-orchestrator/scripts/lint-platforms.test.py` | 修改 | 加 hub 校验测试 fixture（good + 4 个 bad） |
| `skills/api-orchestrator/SKILL.md` | 修改 | 决策树 [2] 一行 + 模式表备注 + 关键纪律一条 |
| `skills/api-orchestrator/references/orchestration.md` | 修改 | 规划挡流程插 hub 查重步 |
| `skills/api-orchestrator/references/onboarding.md` | 修改 | 步 7 交付清单加回流项 |
| `skills/api-orchestrator/platforms/easyops/README.md` | 修改 | 资料地图加 hub 一行 |
| `skills/api-orchestrator/platforms/easyops/hub/INDEX.yaml` | 新建 | 存量 33 件商品全量登记 |
| hub 下若干文件 | 删/移 | __pycache__ ×2、migration output/、day.py 移 output/other |

---

### Task 1: lint hub 校验（TDD）

**Files:**
- Modify: `skills/api-orchestrator/scripts/lint-platforms.py`（在第 10 段后、`# ---- 报告 ----` 前插第 11 段）
- Test: `skills/api-orchestrator/scripts/lint-platforms.test.py`（bad fixtures 区后追加）

**Interfaces:**
- Consumes: 现有 `ok/warn/err` 闭包、`load_yaml()`、`base` 变量（均已在 main() 里就绪）。
- Produces: lint 对 `hub/INDEX.yaml` 的 8 条校验；后续 Task 6 的 INDEX 骨架必须能过这些校验。规则明细（Task 6 实现者按此对齐）：
  1. hub/ 存在但无 INDEX.yaml → WARN `hub/: 目录存在但缺 INDEX.yaml（商品索引缺失，查重不可用）`
  2. `category` 指向不存在的一级子目录 → ERR `hub/INDEX.yaml items.{id}.category → {category} 不是 hub/ 下实际子目录`
  3. files 双向：索引文件不存在 → ERR `hub/INDEX.yaml items.{id}.files → {fn} 文件不存在`；hub 成品文件不在索引 → ERR `hub/{category}/{fn} 未登记在 INDEX.yaml（查重不可见）`。成品识别：扩展名 ∈ {zip, tar.gz, gz, xlsx, whl, sh, py, md, json}，跳过 `.gitkeep`、`__pycache__/`
  4. id 重复 → ERR `hub/INDEX.yaml items id 重复: {id}`
  5. scenario 空/缺 → WARN `hub/INDEX.yaml items.{id}: 缺 scenario（语义查重核心字段）`
  6. 版本单份：同 name（或同 id）匹配到多个 `_v<版本>.` 文件 → ERR `hub/{category}/ 同商品多版本并存: ...`
  7. history 一致：`version` ≠ history 末条 `ver`（version 非空且 history 非空时）→ ERR `hub/INDEX.yaml items.{id}: version={v} 与 history 末条 ver={hv} 不一致`
  8. based_on 闭合：指向的 id 不在 items → ERR `hub/INDEX.yaml items.{id}.based_on → {bid} 不在 items 里`

- [ ] **Step 1: 写失败测试（test.py 追加 hub fixtures）**

在 `lint-platforms.test.py` 的 `# ============ resolve_base 解析链 case` 注释块**之前**插入（注意：write() 用 textwrap.dedent，body 顶格写）：

```python
        # ============ hub 校验 case（INDEX 一致性）============
        # 最小 good：hub + INDEX 齐全自洽 → exit 0
        hg = os.path.join(base, "test-hub-good")
        write(os.path.join(hg, "README.md"), "# x\n")
        write(os.path.join(hg, "systems.yaml"), """
            deployment: test-hub-good
            systems:
              sys:
                description: ok
                spec: sys.yaml
        """)
        write(os.path.join(hg, "sys.yaml"), """
            spec: api-cli/v1
            service: { name: sys, default_endpoint: be, endpoints: { be: { base_url: http://x, auth: none } } }
            resources:
              widget:
                description: w
                operations:
                  read: { method: GET, path: "/{id}" }
        """)
        write(os.path.join(hg, "objects.yaml"), """
            objects:
              widget:
                api: widget
                source: sys.yaml:1
                fields:
                  id: { type: string }
        """)
        write(os.path.join(hg, "hub", "kits", "acme-kit_v1.0.1.zip"), "fake")
        write(os.path.join(hg, "hub", "INDEX.yaml"), """
            items:
              - id: acme-kit
                name: ACME套件
                category: kits
                files: [acme-kit_v1.0.1.zip]
                version: 1.0.1
                history:
                  - {ver: 1.0.1, date: 2026-09-07, change: 初版入库}
                scenario: ACME 设备监控接入
                notes: ""
                added: 2026-09-07
        """)
        rc, out = run(base, "test-hub-good")
        if rc != 0 or "[ERR]" in out:
            fails.append(f"[hub-good] 期望 exit 0 无 ERR，实际 rc={rc}\n{out}")

        # bad-1：hub 存在但无 INDEX → WARN（不 ERR）
        hb1 = os.path.join(base, "test-hub-noindex")
        write(os.path.join(hb1, "README.md"), "# x\n")
        write(os.path.join(hb1, "systems.yaml"), "deployment: test-hub-noindex\nsystems:\n  sys:\n    spec: sys.yaml\n")
        write(os.path.join(hb1, "sys.yaml"), """
            spec: api-cli/v1
            service: { name: sys, default_endpoint: be, endpoints: { be: { base_url: http://x, auth: none } } }
            resources:
              widget:
                operations:
                  read: { method: GET, path: "/{id}" }
        """)
        write(os.path.join(hb1, "hub", "kits", "a.zip"), "fake")
        rc, out = run(base, "test-hub-noindex")
        if rc != 0:
            fails.append(f"[hub-noindex] 缺 INDEX 应只 WARN 不 ERR，实际 rc={rc}\n{out}")
        elif "INDEX.yaml" not in out:
            fails.append(f"[hub-noindex] 期望 WARN 提及 INDEX.yaml\n{out}")

        # bad-2：七宗错集齐（category 悬空/文件不存在/未登记/id 重复/多版本/history 不一致/based_on 悬空）→ exit 1 全抓
        hb2 = os.path.join(base, "test-hub-bad")
        write(os.path.join(hb2, "README.md"), "# x\n")
        write(os.path.join(hb2, "systems.yaml"), "deployment: test-hub-bad\nsystems:\n  sys:\n    spec: sys.yaml\n")
        write(os.path.join(hb2, "sys.yaml"), """
            spec: api-cli/v1
            service: { name: sys, default_endpoint: be, endpoints: { be: { base_url: http://x, auth: none } } }
            resources:
              widget:
                operations:
                  read: { method: GET, path: "/{id}" }
        """)
        write(os.path.join(hb2, "hub", "kits", "x-kit_v1.0.1.zip"), "fake")   # 未登记
        write(os.path.join(hb2, "hub", "kits", "y-kit_v1.0.1.zip"), "fake")   # 多版本成员1
        write(os.path.join(hb2, "hub", "kits", "y-kit_v1.0.2.zip"), "fake")   # 多版本成员2
        write(os.path.join(hb2, "hub", "INDEX.yaml"), """
            items:
              - id: x-kit
                name: X套件
                category: ghost-dir            # 悬空类目
                files: [x-kit_v1.0.1.zip, missing.zip]   # missing 不存在
                version: 1.0.1
                history:
                  - {ver: 1.0.0, date: 2026-09-07, change: 初版}   # 末条 1.0.0 ≠ version 1.0.1
                scenario: X 设备
                added: 2026-09-07
              - id: x-kit                      # 重复 id
                name: X套件2
                category: kits
                files: [y-kit_v1.0.1.zip, y-kit_v1.0.2.zip]   # 两版本并存
                version: 1.0.2
                history:
                  - {ver: 1.0.2, date: 2026-09-07, change: v2}
                based_on: ghost-base           # 悬空谱系
                scenario: Y 设备
                added: 2026-09-07
        """)
        rc, out = run(base, "test-hub-bad")
        if rc == 0:
            fails.append(f"[hub-bad] 期望 exit 1，实际 {rc}\n{out}")
        else:
            for kw in ["ghost-dir", "missing.zip", "未登记", "id 重复", "多版本并存", "不一致", "ghost-base"]:
                if kw not in out:
                    fails.append(f"[hub-bad] 期望输出含「{kw}」\n{out}")
```

同时更新文件尾部成功打印（`print("✓ lint 自测通过：...`行）追加 hub 说明：
`✓ ... + hub 校验（good 放行 / noindex WARN / bad 七宗错全抓）`

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.test.py`
Expected: `❌ lint 自测失败`，失败项含 `[hub-bad] 期望 exit 1`（lint 还没有 hub 段，bad fixture exit 0）和 `[hub-noindex] 期望 WARN 提及 INDEX.yaml`。`[hub-good]` 可能已通过（无校验=无 ERR）——正常，以 bad 失败为准。

- [ ] **Step 3: 实现 lint 第 11 段**

在 `lint-platforms.py` 的 `    # ---- 报告 ----` 行**之前**插入（与既有段落同缩进风格，复用 ok/warn/err/load_yaml/base）：

```python
    # ---- 11. hub 商店索引校验（hub/ 存在才跑；INDEX 一致性，机制见 references/hub.md）----
    hub_dir = os.path.join(base, "hub")
    if os.path.isdir(hub_dir):
        idx_path = os.path.join(hub_dir, "INDEX.yaml")
        if not os.path.isfile(idx_path):
            warn("hub/: 目录存在但缺 INDEX.yaml（商品索引缺失，查重不可用）")
        else:
            d, e = load_yaml(idx_path)
            if e:
                err(f"hub/INDEX.yaml: {e}")
            else:
                items = (d or {}).get("items") or []
                # 类目 = hub/ 一级子目录
                cats = {c for c in os.listdir(hub_dir)
                        if os.path.isdir(os.path.join(hub_dir, c)) and not c.startswith(".")}
                # 成品扩展名（配套 .gitkeep 等不算）
                ARTIFACT_EXT = (".zip", ".tar.gz", ".gz", ".xlsx", ".whl", ".sh", ".py", ".md", ".json")
                def is_artifact(fn):
                    return fn.endswith(ARTIFACT_EXT) and fn != ".gitkeep"
                seen_ids, id_by_file = {}, {}
                for it in items:
                    if not isinstance(it, dict):
                        continue
                    iid = it.get("id") or "<无id>"
                    # 规则4: id 唯一
                    if iid in seen_ids:
                        err(f"hub/INDEX.yaml items id 重复: {iid}")
                    seen_ids[iid] = True
                    # 规则2: category 合法
                    cat = it.get("category")
                    if cat and cat not in cats:
                        err(f"hub/INDEX.yaml items.{iid}.category → {cat} 不是 hub/ 下实际子目录")
                    # 规则3a: 索引文件存在
                    for fn in (it.get("files") or []):
                        if cat and not os.path.isfile(os.path.join(hub_dir, str(cat), str(fn))):
                            err(f"hub/INDEX.yaml items.{iid}.files → {fn} 文件不存在")
                        else:
                            id_by_file[str(fn)] = iid
                    # 规则5: scenario 非空
                    if not (it.get("scenario") or "").strip():
                        warn(f"hub/INDEX.yaml items.{iid}: 缺 scenario（语义查重核心字段）")
                    # 规则7: version 与 history 末条一致
                    ver, hist = it.get("version"), it.get("history") or []
                    if ver and hist and isinstance(hist[-1], dict):
                        hv = str(hist[-1].get("ver", ""))
                        if hv and str(ver) != hv:
                            err(f"hub/INDEX.yaml items.{iid}: version={ver} 与 history 末条 ver={hv} 不一致")
                    # 规则8: based_on 闭合（两遍扫描做不到前向引用，先收集，循环外补查）
                all_ids = set(seen_ids)
                for it in items:
                    if isinstance(it, dict) and it.get("based_on") and it["based_on"] not in all_ids:
                        err(f"hub/INDEX.yaml items.{it.get('id')}.based_on → {it['based_on']} 不在 items 里")
                # 规则3b: hub 成品文件全部在索引（跳过 .gitkeep/__pycache__）
                for c in sorted(cats):
                    cdir = os.path.join(hub_dir, c)
                    for root, dirs, fns in os.walk(cdir):
                        dirs[:] = [x for x in dirs if x != "__pycache__"]
                        for fn in fns:
                            if not is_artifact(fn):
                                continue
                            if fn not in id_by_file:
                                err(f"hub/{c}/{fn} 未登记在 INDEX.yaml（查重不可见）")
                    # 规则6: 同目录下同商品多版本并存（_vX.Y.Z 文件名前缀相同）
                    import re as _re6
                    vers = {}
                    for fn in os.listdir(cdir):
                        m = _re6.match(r"^(.+)_v(\d+\.\d+\.\d+)", fn)
                        if m:
                            vers.setdefault(m.group(1), []).append(fn)
                    for base_name, vfn in vers.items():
                        if len(vfn) > 1:
                            err(f"hub/{c}/ 同商品多版本并存: {', '.join(sorted(vfn))}（只留最新，旧版 git rm）")
                ok("hub INDEX 一致性校验完成")
```

注意：`id_by_file` 只记"索引声明且文件存在"的文件名（不含目录前缀，因 files 是相对 category 的文件名）。子目录文件（如 `scripts/cmdb/x.py`）登记时写 `files: [cmdb/x.py]`，walk 时需用相对 category 的 posix 路径比对——实现时把 3b 的比对键统一为 `os.path.relpath(fp, cdir).replace(os.sep, "/")`，3a 的存在性检查对含 `/` 的文件名同样成立（`os.path.isfile(hub/<cat>/cmdb/x.py)`）。**实现时按此修正上面 3b 循环**（遍历用 `os.walk` 收集 relpath 再查 `id_by_file`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.test.py`
Expected: `✓ lint 自测通过`（含 hub 校验说明），exit 0。

- [ ] **Step 5: 跑真实 easyops lint 确认不误伤**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.py easyops`
Expected: 现阶段 hub 无 INDEX → 出现 1 条 `[WARN] hub/: 目录存在但缺 INDEX.yaml`，**无新增 ERR**（hub 下 py/json/sh/md 文件的"未登记" ERR 要等规则跑起来——注意：此时会刷出大量 ERR「未登记」！这是**预期的失败状态**，证明规则 3b 生效；Task 6 登记 INDEX 后消除。若要本步绿色，可接受本轮 ERR 并在 commit message 注明"INDEX 登记在 Task 6 补齐"。）

- [ ] **Step 6: Commit**

```bash
cd /workspace && git add skills/api-orchestrator/scripts/lint-platforms.py skills/api-orchestrator/scripts/lint-platforms.test.py
git commit -m "feat(api-orchestrator): lint 第11段 hub 索引一致性校验（8规则+测试）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: references/hub.md（机制唯一定义点）

**Files:**
- Create: `skills/api-orchestrator/references/hub.md`

**Interfaces:**
- Consumes: spec §2（结构）/§3.1（查重）/§3.2（回流）。
- Produces: 后续 Task 3/4/5 文档挂载全部指向本文件（`见 references/hub.md`）；Task 6 INDEX 骨架字段顺序按本文件 schema。

- [ ] **Step 1: 写 hub.md（~110 行，通用零系统耦合）**

全文框架（内容按 spec 展开，此处给关键段落实文）：

```markdown
# hub.md —— 平台商店（成品资产库：查重 + 回流）

> hub 是 `platforms/<deployment>/hub/` 下的**开发态成品资产库**：开发任务开始前先查重（是否已有
> 可复用套件/脚本），成品交付时自动回流。机制通用零耦合——类目语义由各 deployment 的 INDEX.yaml
> 头注释声明，本文件不硬编码任何类目。lint 第 11 段兜底索引一致性。

## 定位（与部署根的关系）

- hub 在 **skill 自带 platforms** 里（开发态资产，随 skill 分发），不是运行时部署根
  （`$PWD/.api-orchestrator/`）——orchestration 模式对 platforms 的只读纪律不变。
- **权限口径**：规划挡"开发交付场景"（产物是可导入/可部署成品）视为开发态延伸——回流是交付
  动作的一部分，不算违反只读纪律。纯查询编排仍禁止写 hub。

## 结构（hub/ = 类目子目录 + INDEX.yaml）

- 一级子目录 = 商品类目（如套件包/脚本/工具……具体语义见本 deployment INDEX.yaml 头注释）。
- `INDEX.yaml` = 唯一商品索引（查重载体 + 变更日志），每件商品一条：

（INSERT：spec §2.2 的 9 字段 YAML 示例原文照抄——id/name/category/files/version/history/
 based_on/scenario/notes/added，含字段注释）

- **命名规范**：`<名称>_v<版本>.<ext>`；新版本回流 → 删旧版文件（git rm），目录只留最新版，
  历史靠 git + INDEX.history 双轨追溯。
- 收录门槛：**仅成品交付物**（可导入/可部署）。中间产物/调试脚本不回流（落 tmp/ 或项目目录）。

## 查重流程（开发前必查，规划挡步 1 后）

触发：需求涉及开发/交付新套件/脚本/工具（非纯查询编排）。

1. `Read $PLATFORMS_ROOT/<dep>/hub/INDEX.yaml`（单文件一次读全）。
2. 拿需求的对象/场景关键词对 `scenario` + `name` 语义匹配。
3. 三分支：
   - **命中** → 报告"hub 已有 <name> v<version>（<category>/<file>）"，给复用/改造/重做建议，
     用户选向后再继续；
   - **近似命中**（同类设备不同型号/同域不同对象）→ 报告差异，建议作改造基底（版本 +1 或
     改出独立 id + based_on），用户决定；
   - **未命中** → 正常开发，交付时回流。
4. 结论写 `tmp/<task>/plan.md`（"hub 查重：未命中 / 复用 <id>"），留审计痕。

查重不改变挡位判定；直通/确认挡不触发。

## 回流流程（交付时自动）

触发：规划挡交付，产物含可导入/可部署成品。

1. **归位**：成品落 `hub/<category>/`，命名 `<名称>_v<版本>.<ext>`；旧版文件同目录删除（git rm）。
2. **登记**（INDEX.yaml items 三种分叉）：
   - 新商品 → 追加一条，history 首行 `{ver, date, change: 初版入库}`；
   - 既有商品升级（不满足需求 → 加功能/修 bug 回流）→ version +1、更新 files、history 追加
     一行（change = 用户需求原话或修复要点一句话）、added 不动；
   - 近似命中改出新商品 → 新条目 + `based_on` 指向基底 id。
3. **commit**：回流后立即 commit（防自动提交机制打包垃圾 message）。
4. 配套文档（告警模板 xlsx/MIB 参考 md 等）跟主件同 files 登记，不单独成商品。

## lint 兜底（第 11 段，hub 存在才跑）

8 条规则速查：INDEX 缺失(WARN) / category 合法(ERR) / files 双向一致(ERR) / id 唯一(ERR) /
scenario 非空(WARN) / 版本单份(ERR) / history 末条与 version 一致(ERR) / based_on 闭合(ERR)。

## 与 onboarding 锁回纪律的差异

hub 是高频回流域：写 hub 前 `chmod -R u+w platforms/<dep>/hub/`，改完**不锁回**（区别于
onboarding 对 platforms 整体的改完锁回）。
```

写作要求：不出现 easyops/套件包等具体系统词（类目举例用中性词或明确标注"由 deployment 声明"）；行数控制在 90-120。

- [ ] **Step 2: 自检零耦合**

Run: `cd /workspace && grep -nE "easyops|EasyOps|cmdb|itsm|inspection|monitor" skills/api-orchestrator/references/hub.md`
Expected: 无输出（退出码 1）。

- [ ] **Step 3: Commit**

```bash
cd /workspace && git add skills/api-orchestrator/references/hub.md
git commit -m "feat(api-orchestrator): references/hub.md 平台商店机制定义（查重+回流+结构）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 机制挂载（SKILL.md + orchestration.md + onboarding.md + platforms README）

**Files:**
- Modify: `skills/api-orchestrator/SKILL.md`（决策树 [2] 区块 ~44-48 行、模式表 ~105-116 行、关键纪律 ~118-127 行）
- Modify: `skills/api-orchestrator/references/orchestration.md`（规划挡流程 ~70-77 行）
- Modify: `skills/api-orchestrator/references/onboarding.md`（步 7 ~192-197 行）
- Modify: `skills/api-orchestrator/platforms/easyops/README.md`（资料地图表 ~19-28 行）

**Interfaces:**
- Consumes: Task 2 的 `references/hub.md`（挂载指针全部指向它）。
- Produces: skill 调度链路上 hub 查重/回流的触发点（Task 7 验收演练依赖）。

- [ ] **Step 1: SKILL.md 决策树 [2] 加一行**

在决策树 `[2]` 块内、`· 跨系统/多步/build/change/插件     → 规划挡` 行之后加：

```
      · 涉及开发/交付套件/脚本/工具成品 → 规划挡 + 开工前先查 hub 商店（见 references/hub.md）
```

- [ ] **Step 2: SKILL.md 模式表加口径备注**

「模式与写保护」节表格下方"写保护纪律"列表末尾追加一条：

```
- **hub 回流口径**：规划挡"开发交付场景"（产物为可导入/可部署成品）视为开发态延伸——交付时自动回流 `platforms/<dep>/hub/`（落文件 + 更新 INDEX.yaml + 立即 commit），不算违反 orchestration 只读纪律；纯查询编排仍禁写 hub。机制详见 `references/hub.md`。
```

- [ ] **Step 3: SKILL.md 关键纪律加一条**

「关键纪律」列表末尾追加：

```
- **开发前查 hub、成品回流 hub**：涉及套件/脚本/工具开发的需求，规划挡开工前先读 `hub/INDEX.yaml` 查重（命中→复用/改造，未命中→开发后回流）；成品交付时落 `hub/<category>/` + 登记 INDEX（含 history 变更行）。详见 `references/hub.md`。
```

- [ ] **Step 4: orchestration.md 规划挡流程插查重步**

「规划挡」编号流程的 1、2 之间插入（原 2-7 顺延为 3-8）：

```
2. **hub 查重（涉及开发/交付成品时）** → 读 `hub/INDEX.yaml` 对 `scenario`+`name` 语义匹配；命中/近似命中 → 报告用户选向（复用/改造/重做）；未命中 → 正常开发。结论记入 plan.md。详见 `references/hub.md`。
```

- [ ] **Step 5: onboarding.md 步 7 加回流项**

步 7 列表（`- 跑 lint` 那条之后）插入：

```
- **成品回流 hub**：本次交付若产出可导入/可部署成品（套件包/脚本/工具），按 `references/hub.md` 回流流程落 `hub/<category>/` + 登记 INDEX.yaml（新商品 history 首行"初版入库"）。
```

- [ ] **Step 6: platforms/easyops/README.md 资料地图加一行**

资料地图表（`| 文件 | 装什么 | 何时查 |`）末尾加：

```
| **hub/INDEX.yaml** | 平台商店商品索引（33 件存量成品：套件包/脚本/模板，含版本演进 history） | 「有没有现成的 XX 套件/脚本」（开发前查重） |
```

- [ ] **Step 7: 校验挂载一致性**

Run: `cd /workspace && grep -rn "references/hub.md" skills/api-orchestrator/SKILL.md skills/api-orchestrator/references/orchestration.md skills/api-orchestrator/references/onboarding.md`
Expected: 4 处命中（SKILL 2 + orchestration 1 + onboarding 1）。

- [ ] **Step 8: Commit**

```bash
cd /workspace && git add skills/api-orchestrator/SKILL.md skills/api-orchestrator/references/orchestration.md skills/api-orchestrator/references/onboarding.md skills/api-orchestrator/platforms/easyops/README.md
git commit -m "feat(api-orchestrator): hub 查重/回流机制挂载（SKILL决策树+三挡位文档+README资料地图）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 存量清理

**Files:**
- Delete: `skills/api-orchestrator/platforms/easyops/hub/scripts/cmdb/__pycache__/`、`skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/__pycache__/`
- Delete: `skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/output/`（export_report.json、import_report.json、instances/）
- Move: `skills/api-orchestrator/platforms/easyops/hub/scripts/day.py` → `/workspace/output/other/day.py`

**Interfaces:**
- Consumes: 无。
- Produces: 干净的 hub 目录（Task 5 INDEX 登记的对象）。

- [ ] **Step 1: 删运行时垃圾**

```bash
cd /workspace && git rm -r --cached skills/api-orchestrator/platforms/easyops/hub/scripts/cmdb/__pycache__ 2>/dev/null; rm -rf skills/api-orchestrator/platforms/easyops/hub/scripts/cmdb/__pycache__ skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/__pycache__
```

（先探 `git ls-files | grep __pycache__` 确认是否被跟踪；未被跟踪则纯 rm。）

- [ ] **Step 2: 删 migration 运行输出**

```bash
cd /workspace && git ls-files skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/output/ | head   # 探是否跟踪
git rm -r skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/output/ 2>/dev/null || rm -rf skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/output/
```

- [ ] **Step 3: day.py 移 output/other**

```bash
cd /workspace && git mv skills/api-orchestrator/platforms/easyops/hub/scripts/day.py output/other/day.py
```

（`output/` 若不在 git 内则 `mv` + `git rm --cached` 对应路径——先 `git ls-files output/other/ | head` 探测。）

- [ ] **Step 4: 验证清理干净**

Run: `cd /workspace && find skills/api-orchestrator/platforms/easyops/hub -name "__pycache__" -o -name "*.pyc" | wc -l && ls skills/api-orchestrator/platforms/easyops/hub/scripts/platform/easyops-migration/ && ls output/other/day.py`
Expected: `0`；migration 下剩 `conf.sample.yaml` + `migration_tool.py`；day.py 在新位置。

- [ ] **Step 5: Commit**

```bash
cd /workspace && git add -A skills/api-orchestrator/platforms/easyops/hub/ output/other/day.py
git commit -m "chore(api-orchestrator): hub 清理运行时垃圾(__pycache__/migration output)，day.py 移 output/other

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: INDEX.yaml 存量全量登记（33 件商品）

**Files:**
- Create: `skills/api-orchestrator/platforms/easyops/hub/INDEX.yaml`

**Interfaces:**
- Consumes: Task 1 的 lint 规则（产物必须 0 ERR 通过）；Task 4 的干净目录（48 文件 → 33 商品，配套文件并入主件 files）。
- Produces: 查重载体（Task 7 验收演练的对象）。

**商品清单与 id 映射表**（files 为相对 category 路径；版本从文件名/包内读出；scenario 依内容物写关键词；added 用 git 首见日期 2026-08-29，day.py 除外无商品）：

cmdb-kits（3 商品）：
1. `svc-cmdb-kit` / SVC监控采集(CMDB) / [svc-cmdb-kit_v1.0.11.zip] / 1.0.11 / scenario: IBM SVC 存储设备 SNMP/REST CMDB 信息采集
2. `fc-switch-cmdb-kit` / 光纤交换机SNMP信息采集 / [光纤交换机SNMP信息采集_v1.0.11.zip, fc_switch_snmp_mib_oid_reference.md] / 1.0.11 / scenario: 光纤交换机(Brocade/华为OEM) SNMP CMDB 配置采集，配套 MIB OID 参考
3. `sangfor-lb-cmdb-kit` / 深信服负载均衡器SNMP信息采集 / [深信服负载均衡器SNMP信息采集_v1.0.4.zip] / 1.0.4 / scenario: 深信服 AD 负载均衡器 SNMP CMDB 信息采集

inspection-kits（10 商品）：
4. `ibmsvc-inspection-kit` / IBMSVC存储设备巡检(ONEMODEL) / [IBMSVC存储设备巡检(ONEMODEL)_v1.0.1.tar.gz] / 1.0.1 / scenario: IBM SVC 存储设备巡检（ONEMODEL 模型体系）
5. `host-inspection-kit` / 主机巡检 / [host.tar.gz] / ""（包内 instanceid 无版本语义，留空）/ scenario: Linux 主机巡检（HOST 模型）
6. `bes-cache-inspection-kit` / BES缓存巡检 / [inspector_bes_cache.tar.gz] / ""/ scenario: BES 缓存中间件巡检
7. `bws-inspection-kit` / BWS巡检 / [inspector_bws.tar.gz] / ""/ scenario: BWS(BroadWeb?)服务巡检
8. `dm-inspection-kit` / 达梦数据库巡检 / [inspector_dm.tar.gz] / ""/ scenario: 达梦 DM 数据库巡检
9. `mysql-inspection-kit` / MySQL巡检 / [inspector_mysql.tar.gz] / ""/ scenario: MySQL 数据库巡检
10. `redfish-inspection-kit` / Redfish硬件巡检 / [inspector_redfish.gz] / ""/ scenario: 服务器 Redfish 硬件巡检（带外管理）
11. `redis-inspection-kit` / Redis巡检 / [inspector_redis.tar.gz] / ""/ scenario: Redis 缓存巡检
12. `switch-inspection-kit` / 交换机巡检(ONEMODEL) / [交换机巡检(ONEMODEL)_v1.0.4.tar.gz] / 1.0.4 / scenario: 网络交换机巡检（SNMP）
13. `sangfor-ad-inspection-kit` / 深信服AD负载均衡器巡检(ONEMODEL) / [深信服AD负载均衡器巡检(ONEMODEL)_v1.0.2.tar.gz] / 1.0.2 / scenario: 深信服 AD 负载均衡器巡检

monitor-kits（5 商品，套件+配套 xlsx 同 files）：
14. `bcs-monitor-kit` / BCS监控套件 / [BCS监控套件.zip, BCS监控套件_告警规则模板.xlsx] / ""/ scenario: BCS 存储/集群监控指标采集+告警规则
15. `bws-monitor-kit` / BWS监控套件 / [BWS监控套件.zip, BWS监控套件_告警规则模板.xlsx] / ""/ scenario: BWS 服务监控指标+告警规则
16. `svc-monitor-kit` / SVC监控套件 / [SVC监控套件_v1.0.10.zip, SVC监控套件_告警规则模板.xlsx] / 1.0.10 / scenario: IBM SVC 存储监控指标+告警规则
17. `fc-switch-monitor-kit` / 光纤交换机监控套件 / [光纤交换机监控套件_v1.0.13.zip, 光纤交换机监控_告警规则模板.xlsx] / 1.0.13 / scenario: 光纤交换机(Brocade/华为OEM) SNMP 指标监控+告警规则
18. `sangfor-ad-monitor-kit` / 深信服AD负载均衡器监控套件 / [深信服AD负载均衡器监控套件_v1.0.1.zip, 深信服AD负载均衡器监控_告警规则模板.xlsx] / 1.0.1 / scenario: 深信服 AD 负载均衡器监控+告警规则

scripts（11 商品；scenario 依脚本 docstring/功能写）：
19. `baseline-checker` / 配置基线管理 / [baseline-managment/README.md, baseline-managment/baseline_checker.py, baseline-managment/baseline_models.json] / ""/ scenario: CMDB 声明式基线规则检查引擎（模型+脚本+结果落 CMDB）
20. `build-model-from-data` / 数据驱动建模 / [cmdb/build_model_from_data.py] / ""/ scenario: 从存量数据 JSON 反推 CMDB 模型定义
21. `import-and-register-users` / 用户导入注册 / [cmdb/import_and_register_users.py] / ""/ scenario: 批量导入用户并注册到 EasyOps
22. `delivery-summary-export` / 交付清单导出 / [delivery_summary_export.py] / ""/ scenario: EasyOps 交付清单汇总导出 Excel
23. `hardware-collect-readiness` / 硬件采集条件统计 / [easyops硬件采集条件统计.zip] / ""/ scenario: 主机硬件采集就绪度检查+离线依赖包（含 pysnmp whl）
24. `iptables-set` / iptables 防火墙配置 / [iptables-set.sh] / ""/ scenario: EasyOps 主机 iptables 端口放行脚本
25. `itsm-cmdb-import` / ITSM 导入 CMDB / [itsm/itsm-import-cmdb.py, itsm/itsm_api_client.py, itsm/itsm_process_doc.py, itsm/pillow-12.2.0-cp312-cp312-manylinux2014_aarch64.manylinux_2_17_aarch64.whl] / ""/ scenario: ITSM 工单流程文档处理+CMDB 导入（含 aarch64 pillow 离线 whl）
26. `easyops-migration` / EasyOps 迁移工具 / [platform/easyops-migration/conf.sample.yaml, platform/easyops-migration/migration_tool.py] / ""/ scenario: EasyOps 跨环境实例迁移（导出/导入报告）
27. `check-auth-token-disable` / 鉴权 token 检查 / [platform/check_auth_token_disable.sh] / ""/ scenario: 检查 EasyOps auth token 禁用状态
28. `curl-sample` / curl 样例 / [platform/curl_sample.sh] / ""/ scenario: EasyOps API curl 调用样例
29. `easyops-hardening` / EasyOps 加固 / [platform/easyops_hardening.sh] / ""/ scenario: EasyOps 主机安全加固脚本
30. `init-easyops-python3` / Python3 环境初始化 / [platform/init_easyops_python3.sh] / ""/ scenario: EasyOps 主机离线初始化 Python3 环境
31. `snmpwalk-alarm-pusher` / snmpwalk 告警推送 / [snmpwalk_alarm_pusher.py] / ""/ scenario: snmpwalk 采集结果转告警推送
32. `sync-instance-field` / 实例字段同步 / [sync_instance_field.py] / ""/ scenario: CMDB 实例字段批量同步
33. `webhook` / webhook 接收器 / [webhook.py] / ""/ scenario: 通用 webhook 接收转发
34. `gongji-alarm-access` / 共济告警接入 / [共济告警接入.zip] / ""/ scenario: 共济基础设施监控告警接入（含 V2.23 标准 PDF + pusher 脚本）

other（1 商品）：
35. `cluster-deploy-checklists` / EasyOps集群部署清单-合集 / [EasyOps集群部署清单-合集.zip] / ""/ scenario: EasyOps 集群 1/3/5/7 节点部署清单 md 合集

（编号到 35 但 id 映射实际 34 条目 → 上面编号有笔误空间，**实现时以清点目录实际文件为准**逐条对齐 lint 双向校验；上表是登记指南不是逐字脚本。）

- [ ] **Step 1: 写 INDEX.yaml**

头部注释 + `items:` 全量。骨架（每条目字段顺序统一 id/name/category/files/version/history/scenario/notes/added；history 一律 `[{ver: <version或空>, date: 2026-08-29, change: 初版入库}]`——version 空的条目 history 写 `[{date: 2026-08-29, change: 初版入库}]` 以免触发规则 7；`notes` 简短或空串）：

```yaml
# EasyOps hub 平台商店索引 —— 查重载体 + 变更日志（机制见 skill references/hub.md）
# 类目语义: cmdb-kits=CMDB采集套件包 inspection-kits=巡检套件包(tar.gz)
#   monitor-kits=监控套件包+配套告警规则模板(xlsx) scripts=可交付脚本(按域分子目录)
#   tools=autoops 工具成品 sso-adapter-providers=sso provider 成品
#   itsm-service=ITSM 服务/流程交付包 custom-msg-sender=自定义消息发送器 other=未归类交付物
# 收录门槛: 仅成品交付物；命名 <名称>_v<版本>.<ext>；新版回流删旧版文件
items:
  - id: svc-cmdb-kit
    name: SVC监控采集(CMDB)
    category: cmdb-kits
    files: [svc-cmdb-kit_v1.0.11.zip]
    version: 1.0.1.1→照抄清单表 1.0.11
    history:
      - {ver: 1.0.11, date: 2026-08-29, change: 初版入库}
    scenario: IBM SVC 存储设备 SNMP/REST CMDB 信息采集
    notes: ""
    added: 2026-08-29
  # ……其余 34 条同构
```

（上面 version 行是示意笔误警示——实际写 `version: 1.0.11`；每条 history 与 version 保持一致。）

- [ ] **Step 2: lint 验证 0 ERR**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.py easyops 2>&1 | tail -15`
Expected: `合计: N OK, M WARN, 0 ERR`，退出码 0。若有 ERR 按报错修 INDEX（多为文件名/路径笔误）。

- [ ] **Step 3: 抽查语义质量**

Run: `cd /workspace && grep -c "scenario:" skills/api-orchestrator/platforms/easyops/hub/INDEX.yaml`
Expected: 与 items 条数一致（无漏 scenario）。

- [ ] **Step 4: Commit**

```bash
cd /workspace && git add skills/api-orchestrator/platforms/easyops/hub/INDEX.yaml
git commit -m "feat(api-orchestrator): hub INDEX.yaml 存量全量登记（35 件商品含 history）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 端到端验收

**Files:**
- 无新文件（验证 Task 1-5 的集成效果）

**Interfaces:**
- Consumes: 全部前置任务产物。
- Produces: 验收结论（写入本计划文件尾部或 commit message）。

- [ ] **Step 1: lint 全绿**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.py easyops; echo "exit=$?"`
Expected: `0 ERR`，`exit=0`（WARN 可留：README 引用等既有 WARN）。

- [ ] **Step 2: 自测套件全绿**

Run: `cd /workspace && python3 skills/api-orchestrator/scripts/lint-platforms.test.py; echo "exit=$?"`
Expected: `✓ lint 自测通过`，`exit=0`。

- [ ] **Step 3: 查重演练（人工，LLM 纪律验证）**

模拟需求："要做一个光纤交换机的监控接入"。按 orchestration.md 规划挡新流程走：读 INDEX → 应命中 `fc-switch-monitor-kit`（scenario 含"光纤交换机 SNMP 指标监控"）→ 输出"hub 已有 光纤交换机监控套件 v1.0.13（monitor-kits/光纤交换机监控套件_v1.0.13.zip），建议复用或改造"。
验证点：INDEX 的 scenario 关键词确实能支撑该匹配（grep 确认关键词在位）。

Run: `cd /workspace && grep -n "光纤交换机" skills/api-orchestrator/platforms/easyops/hub/INDEX.yaml | head -5`
Expected: cmdb-kits 与 monitor-kits 两条命中（查重时能区分采集/监控两类）。

- [ ] **Step 4: 回流演练（人工，走一遍新商品分叉）**

模拟：给 `iptables-set` 加功能后回流 v1.0.1 → 文件改名 `iptables-set_v1.0.1.sh`（原无版本名删除）、INDEX 条目 version: 1.0.1 + history 追加 `{ver: 1.0.1, date: <今日>, change: 演练：增加 XX}` → 跑 lint 确认 0 ERR → **演练产物回滚**（git checkout 恢复文件名 + 还原 INDEX 条目），不留演练痕迹。

- [ ] **Step 5: 收尾 commit（若有演练残留修复）**

```bash
cd /workspace && git status --short skills/api-orchestrator/   # 应干净
```
Expected: 无输出（Task 1-5 已各自 commit；演练已回滚）。

---

## Self-Review 记录

- **Spec 覆盖**：§2.1 类目→Task 5 头注释 + Task 2；§2.2 INDEX schema→Task 2/5；§2.3 清理→Task 4；§3.1 查重→Task 2/3/6；§3.2 回流→Task 2/3/6；§3.3 lint→Task 1；§3.4 挂载→Task 3；§3.5 测试→Task 1/6。无缺口。
- **占位符**：Task 2 的 hub.md 框架含两处 "INSERT/照抄" 指令——这是有意的"从 spec 原文照抄"指引而非 TBD；Task 5 商品表明确标注"以实际清点为准"。可接受。
- **类型一致**：lint 错误消息关键词与测试断言关键词逐一对齐（ghost-dir/missing.zip/未登记/id 重复/多版本并存/不一致/ghost-base）；INDEX 字段名 9 个在 Task 1/2/5 三处一致。
